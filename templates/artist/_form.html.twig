{{ form_start(form) }}
{{ form_widget(form) }}
<div class="d-flex justify-content-between">
    <button type="submit" class="btn btn-info">{{ button_label|default('Ajouter artiste') }}</button>
</div>
{{ form_end(form) }}