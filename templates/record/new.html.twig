{% extends 'base.html.twig' %}

{% block title %}New Record{% endblock %}

{% block body %}
<h1 class="mt-3 text-center text-white">Ajouter nouvel album</h1>
<div class="d-flex justify-content-center text-white">
    <div class="col-9 col-md-6">
        <div class="p-2 my-5 shadow-lg rounded-2">

            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn btn-info">{{ button_label|default('Ajouter album') }}</button>
            {{ form_end(form) }}
        </div>
    </div>
</div>
{% endblock %}