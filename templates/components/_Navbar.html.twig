<nav class="navbar bg-secondary">
    <div class="container-fluid px-0 d-flex flex-row justify-content-between align-items-center">
        <div>
            <a href="{{ path('app_home')}}">
                <img src="{{ asset('build/images/vinyle.png')}}" alt="logo" class="btn logo img-fluid">
            </a>
        </div>
        <div class="d-none d-lg-block">
            <!-- Display "Nouvel artiste" button for non-responsive layout (>= lg) -->
            <ul class="list-inline">
                <li class="list-inline-item"><a class="btn btn-info m-2 shadow-lg text-white" href="{{ path('app_artist_new')}}">Nouvel artiste</a></li>
                <li class="list-inline-item"><a class="btn text-white" href="{{ path('app_artist_index')}}">Artistes</a></li>
                <li class="list-inline-item"><a class="btn text-white" href="{{ path('app_record_index')}}">Albums</a></li>
            </ul>
        </div>
        <div class="d-lg-none">
            <!-- Display burger menu for responsive layout (<= lg) -->
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMenu" aria-controls="navbarMenu" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
        </div>
    </div>
    <div class="collapse navbar-collapse" id="navbarMenu">
        <ul class="navbar-nav">
            <li class="nav-item"><a class="btn btn-info m-2 shadow-lg text-white" href="{{ path('app_artist_new')}}">Nouvel artiste</a></li>
        </ul>
        <ul class="navbar-nav">
            <li class="nav-item"><a class="nav-link text-white" href="{{ path('app_artist_index')}}">Artistes</a></li>
            <li class="nav-item"><a class="nav-link text-white" href="{{ path('app_record_index')}}">Albums</a></li>
        </ul>
        
    </div>
</nav>